interface:
  display_name: "KV STUDIO Operator"
  short_description: "Run KV STUDIO through scaffold, guard, and artifact gates."
  default_prompt: "Use this skill to create or validate a KV STUDIO scaffold, run the guarded scaffold runner, and report same-run artifacts."
